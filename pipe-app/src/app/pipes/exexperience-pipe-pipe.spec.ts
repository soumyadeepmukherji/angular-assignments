import { ExexperiencePipePipe } from './exexperience-pipe-pipe';

describe('ExexperiencePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExexperiencePipePipe();
    expect(pipe).toBeTruthy();
  });
});
