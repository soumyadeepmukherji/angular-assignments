import { SkillcountPipePipe } from './skillcount-pipe-pipe';

describe('SkillcountPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SkillcountPipePipe();
    expect(pipe).toBeTruthy();
  });
});
